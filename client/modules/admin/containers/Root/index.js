import React, { Fragment } from 'react';

import { LogoutButton } from '../../../auth';

const Root = () => (
  <Fragment>
    <LogoutButton />
    <h1>Admin Panel Root</h1>
  </Fragment>
);

export default Root;
