import connected from '../utils/connected';

export const up = connected(async () => {
  // Replace with migration up code
});

export const down = connected(async () => {
  // Replace with migration down code
});
